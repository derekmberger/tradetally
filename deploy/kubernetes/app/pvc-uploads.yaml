apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tradetally-uploads
  namespace: tradetally
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
